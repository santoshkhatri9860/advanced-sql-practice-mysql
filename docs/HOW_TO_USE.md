# How to Use This Repo (MySQL 8)

This repo is **practice-first**:
1) Do the prompt in `exercises/`
2) Check your work in `solutions/`

---

## Option A (Recommended): Use Docker (fast + clean)
### 1) Requirements
- Docker Desktop installed

### 2) Start MySQL
From repo root:
```bash
docker compose up -d
docker compose ps

